This is a silly little digispark Arduino sketch to implement a ghost keyboard
that runs notepad (if plugged into a Windoze machine) and types a message.

make - Runs the arduino build via Docker

Flash to a digispark device using e.g. micronucleus

Happy birthday, Chris!
